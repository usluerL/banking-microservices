
server:
  port: 8071
spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/usluerL/banking-micro-config
          clone-on-start: true
          default-label: main


